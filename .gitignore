.vscode
.idea/
.venv/
venv/
__pycache__/
*.pyc
*.egg-info/
.coverage
.pytest_cache/
.DS_Store

# local scratch — never committed (org rule)
TODO.md
ROADMAP.md

# local benchmark outputs (published to HF, not to git)
predictions/

# node / astro
node_modules/
frontend-static/dist/
frontend-static/.astro/

# python build artifacts (test_packaging.py builds wheels into tmp_path
# normally, but a repo-root `uv build`/pre-push hook run can leave these)
build/
dist/
*.egg-info/
